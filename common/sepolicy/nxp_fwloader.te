type nxp_fwloader, domain;
type nxp_fwloader_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(nxp_fwloader)

allow nxp_fwloader hci_attach_dev:chr_file { open read write ioctl getattr };
